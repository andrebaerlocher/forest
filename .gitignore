node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
# NOT /dist — it is committed on purpose. Consumers install this repo over
# git, and bun neither installs a git dependency's devDependencies nor runs
# its prepare script, so a build-on-install would leave them with no library.

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
.svelte-doctor/*

*storybook.log
storybook-static

# Agents
.agents
.claude
.serena
.svelte-doctor
CLAUDE.md
# src/routes/cases/

# Codegraph local index cache
/.codegraph
.svelte_ide/parse_cache.bin
